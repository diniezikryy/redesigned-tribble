'use client'

import withAuth from "@/components/hoc/withAuth";

function AttemptReviewPage() {
  return (
    <div>
      Attempt review page for attempt id:
    </div>
  )
}

export default withAuth(AttemptReviewPage);